*** Begin Patch
*** Update File: src/components/TicketList.tsx
@@
-        const data = (await api.get(`/tickets?page=${currentPage}`)) as {
-          tickets: SmallTicket[];
-        };
-        const tickets = data.tickets || [];
+        const data = await api.get<{ tickets: SmallTicket[] }>(
+          `/tickets?page=${currentPage}`,
+        );
+        const tickets = data.tickets || [];
*** End Patch
***
*** Begin Patch
*** Update File: src/components/SearchModal.tsx
@@
-          const data = (await api.get("/tickets?page=1")) as {
-            tickets: SmallTicket[];
-          };
+          const data = await api.get<{ tickets: SmallTicket[] }>("/tickets?page=1");
           if (!isMounted) return;
           const tickets = data.tickets || [];
@@
-      if (!latestTicketNumber || query.length !== 3) {
+      if (!latestTicketNumber || query.length !== 3) {
         // Fallback to simple search if no latest ticket number or not 3 digits
-        const data = (await api.get(
-          `/tickets?number=${encodeURIComponent(query)}`,
-        )) as { tickets: SmallTicket[] };
-        setResults(data.tickets || []);
+        const data = await api.get<{ tickets: SmallTicket[] }>(
+          `/tickets?number=${encodeURIComponent(query)}`,
+        );
+        setResults(data.tickets || []);
         return;
       }
@@
-          apiCalls.push(
-        (
-          api.get(`/tickets?number=${number}`) as Promise<{
-            tickets: SmallTicket[];
-          }>
-        )
-          .then((data) => {
-            const tickets = data.tickets || [];
-            // Each GET returns at most one ticket, so return the first one if it exists
-            return tickets.length > 0 ? tickets[0] : null;
-          })
+          apiCalls.push(
+            api
+              .get<{ tickets: SmallTicket[] }>(`/tickets?number=${number}`)
+              .then((data) => {
+                const tickets = data.tickets || [];
+                // Each GET returns at most one ticket, so return the first one if it exists
+                return tickets.length > 0 ? tickets[0] : null;
+              })
               .catch((error) => {
                 console.error(`Error fetching ticket ${number}:`, error);
                 return null;
               }),
-      );
+          );
     }
@@
-          setSearchType("customers");
-          const data = (await api.get(
-            `/customers/autocomplete?query=${encodeURIComponent(phoneDigits)}`,
-          )) as { customers: Customer[] };
-          setResults(data.customers || []);
+          setSearchType("customers");
+          {
+            const data = await api.get<{ customers: Customer[] }>(
+              `/customers/autocomplete?query=${encodeURIComponent(phoneDigits)}`,
+            );
+            setResults(data.customers || []);
+          }
@@
-          setSearchType("tickets");
-          const data = (await api.get(
-            `/tickets?number=${encodeURIComponent(trimmedQuery)}`,
-          )) as { tickets: SmallTicket[] };
-          setResults(data.tickets || []);
+          setSearchType("tickets");
+          {
+            const data = await api.get<{ tickets: SmallTicket[] }>(
+              `/tickets?number=${encodeURIComponent(trimmedQuery)}`,
+            );
+            setResults(data.tickets || []);
+          }
@@
-          setSearchType("customers");
-          const data = (await api.get(
-            `/customers/autocomplete?query=${encodeURIComponent(phoneDigits)}`,
-          )) as { customers: Customer[] };
-          setResults(data.customers || []);
+          setSearchType("customers");
+          {
+            const data = await api.get<{ customers: Customer[] }>(
+              `/customers/autocomplete?query=${encodeURIComponent(phoneDigits)}`,
+            );
+            setResults(data.customers || []);
+          }
@@
-            const [customersData, ticketsData] = await Promise.all([
-              api.get(
-                `/customers/autocomplete?query=${encodeURIComponent(trimmedQuery)}`,
-              ) as Promise<{ customers: Customer[] }>,
-              api.get(
-                `/tickets?query=${encodeURIComponent(trimmedQuery)}`,
-              ) as Promise<{ tickets: SmallTicket[] }>,
-            ]);
-
-            const customers = customersData.customers || [];
-            const tickets = ticketsData.tickets || [];
+            const [customersData, ticketsData] = await Promise.all([
+              api.get<{ customers: Customer[] }>(
+                `/customers/autocomplete?query=${encodeURIComponent(trimmedQuery)}`,
+              ),
+              api.get<{ tickets: SmallTicket[] }>(
+                `/tickets?query=${encodeURIComponent(trimmedQuery)}`,
+              ),
+            ]);
+
+            const customers = customersData.customers || [];
+            const tickets = ticketsData.tickets || [];
*** End Patch
***
*** Begin Patch
*** Update File: src/components/TicketView.tsx
@@
-      const data = (await api.get(`/tickets/${id}`)) as {
-        ticket: LargeTicket;
-      };
-      if (!fetchTicketRef.current.isMounted) return;
-      const ticketData = data.ticket;
+      const data = await api.get<{ ticket: LargeTicket }>(`/tickets/${id}`);
+      if (!fetchTicketRef.current.isMounted) return;
+      const ticketData = data.ticket;
       setTicket(ticketData);
*** End Patch
***
*** Begin Patch
*** Update File: src/components/TicketEditor.tsx
@@
-        result = (await api.put(`/tickets/${ticketId}`, updatedTicket)) as {
-          ticket: LargeTicket;
-        };
+        result = await api.put<{ ticket: LargeTicket }>(
+          `/tickets/${ticketId}`,
+          updatedTicket,
+        );
@@
-        result = (await api.post(`/tickets`, payload)) as {
-          ticket: LargeTicket;
-        }; // create the ticket
+        result = await api.post<{ ticket: LargeTicket }>(`/tickets`, payload);
*** End Patch
***
*** Begin Patch
*** Update File: src/App.tsx
@@
-      const result = (await api.get("/users")) as { users: CognitoUser[] };
+      const result = await api.get<{ users: CognitoUser[] }>("/users");
*** End Patch
*** End Patch